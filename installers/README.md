This directory is no longer used by `scripts/build_gui_exe.ps1`.

`RdcAutomation.exe` now bundles the local `renderdoc_mcp` source tree and installs
the RenderDoc extension from that source. Do not place `RenderDocMCP-Setup-*.exe`
here for GUI builds.
